---
import { shareLinks } from '@/constants'
---

<div
  id='share'
  class='bg-foreground text-background fixed right-7 bottom-7 flex flex-col gap-2 p-3 text-left opacity-0 transition-opacity duration-150 md:right-10 md:bottom-10 md:p-5'
>
  {
    shareLinks.map((link) => (
      <a
        href={link.href(Astro.url.href)}
        target='_blank'
        class='hover-underline-animation-widget w-fit text-sm md:text-base'
      >
        {link.label}
      </a>
    ))
  }
</div>
