---
import type { CollectionEntry } from 'astro:content'
import { getPosts } from '@/lib'
import { generateBlogListJsonLD } from '@/components/shared/jsonLD'
import PageLayout from '@/layouts/PageLayout.astro'
import Heading from '@/components/shared/Heading.astro'
import ItemList from '@/components/shared/ItemList.astro'

const allBlogPosts: CollectionEntry<'blog'>[] = await getPosts('en')
const ld = generateBlogListJsonLD(allBlogPosts, 'en')
---

<PageLayout
  description='Explore una recopilación de mis mejores esfuerzos en Diseño y Desarrollo Web. En un panorama digital dinámico'
  title='Adrian - Blog'
  label='Home'
  link='/en/'
  ld={ld}
>
  <Heading lang='en' page='blog' />
  <ItemList list={allBlogPosts} lang='en' />
</PageLayout>
